import * as THREE from 'three';

// Stubbed HallucinationManager: alucinaciones eliminadas por pedido.
export class HallucinationManager {
    constructor(scene, camera) {
        this.scene = scene;
        this.camera = camera;
    }

    enterForestMode() {}
    exitForestMode() {}
    update(delta) {}
}
